---
"mattpocock-skills": patch
---

Make **`grilling`** speak the user's language. The session now runs in whatever language the user is writing in — every question and summary stays in that language instead of switching to English. This flows through to `grill-me` and `grill-with-docs`, which both drive the `grilling` loop.
